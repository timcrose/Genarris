[Genarris_master]
procedures = ["Cluster_Based_Selection"]
processes_limit = 20
utput_format = json

[cluster_based_selection]
structure_dir = ./structures_evaluated
structure_suffix = .json
number_selected = ??? ; Update with # of clusters generated with fixed silhouette
cluster_property_name = AP_RCD
preference_property_name = FAKE_ENERGY
output_dir = ./structures_selected
output_format = json

